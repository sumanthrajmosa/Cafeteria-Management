# API URL - defaults to /api for local development (Vite proxy handles it)
# For production (Vercel), set VITE_API_URL to your Railway backend URL
# Example: VITE_API_URL=https://your-app.up.railway.app/api
VITE_API_URL=/api
